apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - ../../base

labels:
  - includeSelectors: false
    pairs:
      environment: dev

patches:
  - path: replica-patch.yaml
    target:
      kind: Deployment
      name: order-service